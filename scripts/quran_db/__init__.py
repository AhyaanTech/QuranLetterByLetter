"""Quran Database Builder Package

Builds and validates the offline Quran database from source files.

Usage:
    uv run scripts/build_db.py          # Build database
    uv run scripts/build_db.py --validate   # Validate existing database
    uv run scripts/build_db.py --full       # Build + validate
"""

__version__ = "2.0.0"
