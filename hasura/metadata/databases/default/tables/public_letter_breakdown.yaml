table:
  name: letter_breakdown
  schema: public
object_relationships:
  - name: word
    using:
      foreign_key_constraint_on: word_id
select_permissions:
  - role: public
    permission:
      columns: '*'
      filter: {}
      allow_aggregations: true
